import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#2e7d32', // A green shade for Malenadu vibe
    },
    secondary: {
      main: '#ff9800',
    },
  },
});

export default theme;
